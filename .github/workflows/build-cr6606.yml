name: Build OpenWrt CR660X (Master + Passwall / FullCone / WiFi / PPPoE / Argon)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    - name: Checkout this repo (configs only)
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Install build dependencies
      run: |
        sudo apt update
        sudo apt install -y build-essential clang flex bison g++ gawk gcc-multilib \
          gettext git libncurses5-dev libssl-dev python3-distutils rsync unzip zlib1g-dev \
          file wget curl python3

    - name: Clone OpenWrt master
      run: |
        git clone https://github.com/openwrt/openwrt.git -b master --depth 1 openwrt
        cd openwrt
        ./scripts/feeds update -a

    - name: Add extra feeds (Passwall)
      run: |
        cd openwrt
        cat >> feeds.conf.default << "EOF"
        src-git passwall https://github.com/OpenWrt-Passwall/openwrt-passwall
        EOF

        ./scripts/feeds update passwall
        ./scripts/feeds install -a -p passwall
        ./scripts/feeds install -a

    - name: Load CR660X seed config
      run: |
        cd openwrt
        cp ../config/cr660x.seed .config
        make defconfig

    - name: Show final target info
      run: |
        cd openwrt
        echo "==== TARGET CHECK ===="
        grep CONFIG_TARGET_ .config || true
        grep CR660X .config || true

    - name: Compile firmware
      run: |
        cd openwrt
        make -j$(nproc) || make -j1 V=s

    - name: Upload firmware artifacts
      uses: actions/upload-artifact@v4
      with:
        name: openwrt-cr660x-master
        path: |
          openwrt/bin/targets/
          openwrt/bin/packages/
